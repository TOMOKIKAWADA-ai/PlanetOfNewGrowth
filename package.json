{
  "name": "search-survive-three",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "three": "^0.177.0",
    "vite": "^6.3.5"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
